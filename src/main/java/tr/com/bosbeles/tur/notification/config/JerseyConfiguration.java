/*
package tr.com.bosbeles.tur.notification.config;

import org.glassfish.jersey.server.ResourceConfig;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import tr.com.bosbeles.tur.notification.controller.NotificationController;

@Configuration
public class JerseyConfiguration extends ResourceConfig {

    @Bean
    ResourceConfig config() {
        ResourceConfig resourceConfig = new ResourceConfig();
        resourceConfig.packages("tr.com.bosbeles.tur.notification.controller");
        register(NotificationController.class);
        return resourceConfig;
    }


 */
/*   public void configureSwagger() {
        this.register(ApiListingResource.class);
        this.register(SwaggerSerializers.class);
        BeanConfig config = new BeanConfig();
        config.setConfigId("spring-jersey-swagger-example");
        config.setTitle("Spring, Jersey, and Swagger Example");
        config.setVersion("1.0.0");
        config.setBasePath("/");
        config.setResourcePackage("com.basaki");
        config.setScan(true);
    }*//*

}*/
